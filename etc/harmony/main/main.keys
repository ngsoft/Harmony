#!/bin/bash
# Receive all the inputs from LIRC
# and performs custom action for each keystroke

case "$1" in
    1)
        echo "case 1"
    ;;
    2|3)
        echo "case 2 or 3"
    ;;
    *)
        echo "default"
    ;;
esac
