#!/bin/bash
# Activity starting script
# is launched when activity starts and if $checkrunning
# inside conf is not already detected

. utils

cmd pushd $HOME
    # Needs 5.1 on my Home Theater
    export AE_SINK=ALSA
    cmd kodi &
cmd popd