#!/bin/bash

prefix=$(dirname "$(pushd $(dirname "${BASH_SOURCE[0]}") > /dev/null 2>&1 && pwd && popd > /dev/null 2>&1)")

if [ ! -e $prefix/usr/lib/harmony/utils ]; then
    >&2 echo "$(basename ${BASH_SOURCE[0]}) cannot find libraries."
    exit 1
fi
. $prefix/usr/lib/harmony/utils


